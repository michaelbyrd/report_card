<section class = "submission_form">
  <%= form_for(@submission) do |f| %>

  <div class="field">
    <%= f.label :repository %><br>
    <%= f.text_area :repository %>
  </div>

  <div class="field">
    <%= f.label :application %><br>
    <%= f.text_area :application %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
  <% end %>
</section>
